version: '3.7'


services:
#  postgres:
#    image: postgres:latest
#    ports:
#      - 5432:5432
#    volumes:
#      - postgres-data:/var/lib/postgresql/data
#
#  nginx:
#    image: nginx:latest
#    ports:
#      - 80:80
#    volumes:
#      - ./nginx/podcast-api.conf:/etc/nginx/conf.d/podcast-api.conf
#      - ./nginx/podcast-pages.conf:/etc/nginx/conf.d/podcast-pages.conf
#    depends_on:
#      - podcast-pages

  showtime-api:
    build: .
    command: "showtime"
    ports:
      - 8080:8080
    volumes:
      - ./src:/opt/cadence13/podcast-api/src
      - ./ext:/opt/cadence13/podcast-api/ext
      - ./conf:/opt/cadence13/podcast-api/conf
    environment:
      CONFIG_URL: 'file:///opt/cadence13/podcast-api/conf/config_dev.json'

#volumes:
#  postgres-data: {}
